# Frontend IA

Planned only: `/[locale]/community`, `/matches`, `/matches/new`, `/matches/[id]`, `/my-matches`, `/requests`, `/players/[id]`, `/teams`. Each requires loading, empty, error and permission states. Add Community to desktop navigation and mobile navigation as a new implemented route only in 11G; do not replace booking home. RTL uses logical spacing and localized dates.
