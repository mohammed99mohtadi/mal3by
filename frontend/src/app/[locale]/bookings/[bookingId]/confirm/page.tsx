import { cookies } from "next/headers";import { redirect,notFound } from "next/navigation";import { api } from "@/lib/api";import { BookingConfirm } from "@/components/booking-confirm";
export default async function Confirm({params}:{params:Promise<{locale:string;bookingId:string}>}){const {locale,bookingId}=await params;const token=(await cookies()).get("mal3by_session")?.value;if(!token)redirect(`/${locale}/login?returnTo=/${locale}/bookings/${bookingId}/confirm`);let booking;try{booking=await api.booking(token,bookingId)}catch{notFound()}return <section className="mx-auto max-w-xl p-6"><h1 className="text-3xl font-black">Confirm reservation</h1><BookingConfirm locale={locale} booking={booking}/></section>}
