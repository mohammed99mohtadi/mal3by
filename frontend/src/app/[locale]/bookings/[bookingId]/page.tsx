import Link from"next/link";import{cookies}from"next/headers";import{redirect}from"next/navigation";import{BookingActions}from"@/components/booking-actions";import{Alert}from"@/components/ui/alert";import{StatusBadge}from"@/components/ui/status-badge";import{Surface}from"@/components/ui/surface";import{api}from"@/lib/api";import{bookingLabel,bookingTone}from"@/lib/booking";import{bookingUx,formatDateTime,formatMoney,statusExplanation}from"@/lib/booking-ux";import{copy,type Locale}from"@/lib/copy";import{ApiError}from"@/lib/types";
export default async function BookingDetail({params}:{params:Promise<{locale:string;bookingId:string}>}){const{locale,bookingId}=await params,l=(locale==="en"?"en":"ar")as Locale,text=copy[l],ux=bookingUx[l],token=(await cookies()).get("mal3by_session")?.value;if(!token)redirect(`/${l}/login?returnTo=/${l}/bookings/${bookingId}`);let booking;try{booking=await api.booking(token,bookingId)}catch(error){if(error instanceof ApiError&&error.status===401)redirect(`/${l}/login?returnTo=/${l}/bookings/${bookingId}`);return <section className="page-wrap max-w-3xl"><h1 className="text-3xl font-black">{text.bookingDetailsTitle}</h1><Alert tone="danger" message={error instanceof ApiError&&error.status===403?ux.serviceError:text.bookingNotFound} className="mt-6"/></section>}return <section className="page-wrap max-w-3xl"><Link className="focus-ring text-sm font-bold text-[var(--brand)]" href={`/${l}/bookings`}>{text.bookingMyBookings}</Link><Surface className="mt-5"><div className="flex flex-wrap items-center justify-between gap-3"><h1 className="text-3xl font-black">{text.bookingDetailsTitle}</h1><StatusBadge status={bookingTone(booking.status)}>{bookingLabel(l,booking.status)}</StatusBadge></div><p className="mt-3" role="status">{statusExplanation(l,booking.status)}</p><dl className="mt-7 grid gap-5 border-t border-[var(--border)] pt-6 sm:grid-cols-2"><div><dt className="text-sm text-[var(--text-muted)]">{text.bookingReference}</dt><dd className="mt-1 font-bold"><bdi>#{booking.id}</bdi></dd></div><div><dt className="text-sm text-[var(--text-muted)]">{text.bookingCourt}</dt><dd className="mt-1 font-bold">{booking.court?.[l==="ar"?"name_ar":"name_en"]??`${text.bookingCourt} #${booking.court_id}`}</dd></div><div><dt className="text-sm text-[var(--text-muted)]">{text.bookingTime}</dt><dd className="mt-1"><time dateTime={booking.start_time}>{formatDateTime(l,booking.start_time)}</time> – <time dateTime={booking.end_time}>{formatDateTime(l,booking.end_time,true)}</time></dd></div>{booking.total_price&&<div><dt className="text-sm text-[var(--text-muted)]">{text.bookingAmount}</dt><dd className="mt-1 font-bold"><bdi>{formatMoney(l,booking.total_price,booking.currency)}</bdi></dd></div>}<div><dt className="text-sm text-[var(--text-muted)]">{ux.created}</dt><dd className="mt-1"><time dateTime={booking.created_at}>{formatDateTime(l,booking.created_at)}</time></dd></div>{booking.hold_expires_at&&<div><dt className="text-sm text-[var(--text-muted)]">{text.bookingHoldExpires}</dt><dd className="mt-1"><time dateTime={booking.hold_expires_at}>{formatDateTime(l,booking.hold_expires_at)}</time></dd></div>}{booking.cancellation_reason&&<div><dt className="text-sm text-[var(--text-muted)]">{ux.reason}</dt><dd className="mt-1 break-words">{booking.cancellation_reason}</dd></div>}</dl>{booking.court&&<Link className="focus-ring mt-6 inline-block font-bold text-[var(--brand)]" href={`/${l}/courts/${booking.court_id}`}>{ux.viewCourt}</Link>}<BookingActions booking={booking} locale={l}/></Surface></section>}
