import { AuthShell } from "@/components/auth-shell"; import { Alert } from "@/components/ui/alert"; import { Button } from "@/components/ui/button"; import { Input } from "@/components/ui/input"; import { copy,type Locale } from "@/lib/copy";
export default async function ForgotPassword({params}:{params:Promise<{locale:string}>}){const{locale}=await params;const safe=(locale==="en"?"en":"ar")as Locale,t=copy[safe];return <AuthShell locale={safe} mode="forgot"><div className="forgot-state"><Input fullWidth disabled name="recovery_email" type="email" inputMode="email" autoComplete="email" dir="ltr" label={t.emailLabel}/><Button className="mt-4" type="button" fullWidth size="lg" disabled>{safe==="ar"?"إرسال رابط إعادة التعيين":"Send reset link"}</Button><Alert className="mt-4" tone="warning" title={t.forgotUnavailableTitle} message={t.forgotPasswordUnavailable}/></div></AuthShell>}
