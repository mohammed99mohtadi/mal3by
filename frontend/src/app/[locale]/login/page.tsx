import { AuthForm } from "@/components/auth-form";export default async function Login({params,searchParams}:{params:Promise<{locale:string}>;searchParams:Promise<{returnTo?:string}>}){const {locale}=await params;const {returnTo}=await searchParams;return <section className="p-8"><h1 className="text-center text-3xl font-black">Log in</h1><AuthForm locale={locale} mode="login" returnTo={returnTo}/></section>}
