import{cookies}from"next/headers";import{redirect}from"next/navigation";import{ComingSoonState,FeatureStatusBanner,ProductPageHeader}from"@/components/product-foundation";import{findProductRoute}from"@/lib/product-routes";import type{Locale}from"@/lib/copy";export default async function NewMatch({params}:{params:Promise<{locale:string}>}){const{locale}=await params,l=(locale==="en"?"en":"ar")as Locale,route=findProductRoute("/matches/new")!;if(!(await cookies()).get("mal3by_session"))redirect(`/${l}/login?returnTo=/${l}/matches/new`);return <section className="page-wrap"><FeatureStatusBanner classification="PARTIAL" locale={l}/><ProductPageHeader title={route.title[l]} description={route.description[l]}/><div className="mt-7"><ComingSoonState route={route} locale={l}/></div></section>}
