import { Skeleton } from "@/components/ui/skeleton";import { Surface } from "@/components/ui/surface";export default function Loading(){return <section className="page-wrap" role="status" aria-label="Loading requests / جارٍ تحميل الطلبات"><Skeleton variant="text" className="h-10 w-64"/><div className="mt-7 space-y-4">{[1,2,3].map(item=><Surface key={item}><Skeleton className="h-24"/></Surface>)}</div></section>}
