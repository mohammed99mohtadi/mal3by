import Link from "next/link";import { api } from "@/lib/api";import { copy, type Locale } from "@/lib/copy";import type { Court } from "@/lib/types";
export default async function Home({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const l=(locale==="en"?"en":"ar") as Locale;const t=copy[l];let courts:Court[]=[];try{courts=(await api.courts()).slice(0,3)}catch{}return <><section className="bg-emerald-950 px-5 py-20 text-white"><div className="mx-auto max-w-6xl"><p className="mb-4 text-sm font-bold text-amber-300">KUWAIT SPORTS</p><h1 className="max-w-2xl text-4xl font-black leading-tight sm:text-6xl">{t.hero}</h1><div className="mt-8 flex gap-3"><Link className="rounded-md bg-amber-400 px-5 py-3 font-bold text-emerald-950 focus-ring" href={`/${l}/courts`}>{t.discover}</Link></div></div></section><section className="mx-auto max-w-6xl px-5 py-14"><h2 className="text-2xl font-black">{t.featured}</h2><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{courts.map(c=><Link key={c.id} href={`/${l}/courts/${c.id}`} className="rounded-md border border-emerald-950/10 bg-white p-5 shadow-sm transition hover:-translate-y-0.5 focus-ring"><p className="font-bold">{l==="ar"?c.name_ar:c.name_en}</p><p className="mt-2 text-sm text-emerald-950/65">{c.area} · {c.price_per_hour} {t.price}</p></Link>)}</div></section></>}
