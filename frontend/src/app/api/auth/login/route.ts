import { NextResponse } from "next/server";
const base=process.env.NEXT_PUBLIC_API_BASE_URL??"http://127.0.0.1:8000/api/v1";
export async function POST(req:Request){const body=await req.json();const res=await fetch(`${base}/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await res.json().catch(()=>({detail:"Login failed"}));if(!res.ok)return NextResponse.json({detail:typeof data.detail==="string"?data.detail:"Login failed"},{status:res.status});const out=NextResponse.json({ok:true});out.cookies.set("mal3by_session",data.access_token,{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",path:"/",maxAge:60*60*24*7});return out}
