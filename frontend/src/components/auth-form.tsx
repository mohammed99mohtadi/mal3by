"use client";import { useState } from "react";import { useRouter } from "next/navigation";import { safeReturnPath } from "@/lib/redirect";
export function AuthForm({locale,mode,returnTo}:{locale:string;mode:"login"|"register";returnTo?:string|null}){const router=useRouter();const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const password=String(form.get("password"));if(password.length<8){setError("Password must be at least 8 characters");return}setLoading(true);const body=Object.fromEntries(form);const endpoint=mode==="login"?"/api/auth/login":"/api/auth/register";const res=await fetch(endpoint,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await res.json();setLoading(false);if(!res.ok){setError(data.detail||"Unable to continue");return}router.push(mode==="login"?safeReturnPath(returnTo??null,locale==="en"?"en":"ar"):`/${locale}/login`);router.refresh()}return <form onSubmit={submit} className="mx-auto mt-8 grid max-w-md gap-4 rounded-md border border-emerald-950/10 bg-white p-6 shadow-sm"><label>{mode==="register"&&<>Full name<input required minLength={2} name="full_name" className="mt-1 w-full rounded border p-3"/></>}</label><label>Email<input required type="email" name="email" className="mt-1 w-full rounded border p-3"/></label>{mode==="register"&&<label>Phone <input name="phone_number" className="mt-1 w-full rounded border p-3"/></label>}<label>Password<input required minLength={8} type="password" name="password" className="mt-1 w-full rounded border p-3"/></label>{error&&<p role="alert" className="text-sm text-red-700">{error}</p>}<button disabled={loading} className="rounded bg-emerald-800 p-3 font-bold text-white disabled:opacity-50">{loading?"…":mode==="login"?"Log in":"Create account"}</button></form>}
