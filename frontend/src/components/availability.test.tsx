import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { beforeEach, describe, expect, it, vi } from "vitest";
import { Availability } from "./availability";
const push=vi.fn();vi.mock("next/navigation",()=>({useRouter:()=>({push})}));
describe("Availability",()=>{beforeEach(()=>{vi.unstubAllGlobals();vi.restoreAllMocks();push.mockReset()});it("passes only selected slot identity in booking URL",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,json:async()=>({slots:[{start_time:"2030-01-01T10:00:00Z",end_time:"2030-01-01T11:00:00Z",available:true}]})}));render(<Availability courtId="9" locale="ar"/>);fireEvent.click(screen.getByRole("button",{name:"Check availability"}));await waitFor(()=>expect(screen.getAllByRole("button").some(button=>button.getAttribute("aria-pressed")!==null)).toBe(true));const slot=screen.getAllByRole("button").find(button=>button.getAttribute("aria-pressed")!==null)!;fireEvent.click(slot);expect(slot).toHaveAttribute("aria-pressed","true");fireEvent.click(screen.getByRole("button",{name:"Continue"}));const url=push.mock.calls[0][0];expect(url).toContain("courtId=9");expect(url).toContain("start=");expect(url).toContain("end=");expect(url).not.toMatch(/price|total_price|status|user_id|owner_id/)});it("does not expose a booking CTA before slots load",()=>{render(<Availability courtId="9" locale="ar"/>);expect(screen.queryByRole("button",{name:"Continue"})).not.toBeInTheDocument()});it("shows safe backend failure",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:false}));render(<Availability courtId="9" locale="ar"/>);fireEvent.click(screen.getByRole("button",{name:"Check availability"}));await waitFor(()=>expect(screen.getByRole("alert")).toHaveTextContent("Availability is unavailable"))})});
