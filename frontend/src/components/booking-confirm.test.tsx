import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { beforeEach, describe, expect, it, vi } from "vitest";
import { BookingConfirm } from "./booking-confirm";
const push = vi.fn(); vi.mock("next/navigation", () => ({ useRouter: () => ({ push }) }));
const booking = { id: 12, court_id: 9, start_time: "2030-01-01T10:00:00Z", end_time: "2030-01-01T11:00:00Z", total_price: "25.00", currency: "KWD", status: "pending_payment" as const, created_at: "2030-01-01T09:00:00Z" };
describe("BookingConfirm", () => { beforeEach(() => { vi.unstubAllGlobals(); vi.restoreAllMocks(); push.mockReset(); }); it("confirms pending hold after success", async () => { vi.stubGlobal("fetch", vi.fn().mockResolvedValue({ ok: true })); render(<BookingConfirm locale="en" booking={booking} />); fireEvent.click(screen.getByRole("button", { name: "Confirm booking" })); await waitFor(() => expect(push).toHaveBeenCalledWith("/en/bookings/12/success")); }); it("shows safe failure", async () => { vi.stubGlobal("fetch", vi.fn().mockResolvedValue({ ok: false })); render(<BookingConfirm locale="en" booking={booking} />); fireEvent.click(screen.getByRole("button", { name: "Confirm booking" })); expect(await screen.findByText("Could not confirm this booking. Please try again.")).toBeInTheDocument(); }); });
