import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { beforeEach, describe, expect, it, vi } from "vitest";
import { BookingForm } from "./booking-form";
const push = vi.fn(); vi.mock("next/navigation", () => ({ useRouter: () => ({ push }) }));
const start = "2030-01-01T10:00:00Z", end = "2030-01-01T11:00:00Z";
describe("BookingForm", () => { beforeEach(() => { vi.unstubAllGlobals(); vi.restoreAllMocks(); push.mockReset(); }); it("sends exact hold payload", async () => { const fetchMock = vi.fn().mockResolvedValueOnce({ ok: true, json: async () => ({ slots: [{ start_time: start, end_time: end, available: true }] }) }).mockResolvedValueOnce({ ok: true, json: async () => ({ id: 12 }) }); vi.stubGlobal("fetch", fetchMock); render(<BookingForm locale="en" courtId="9" start={start} end={end} />); fireEvent.click(screen.getByRole("button", { name: "Reserve slot" })); await waitFor(() => expect(push).toHaveBeenCalledWith("/en/bookings/12/confirm")); expect(JSON.parse(fetchMock.mock.calls[1][1].body)).toEqual({ court_id: 9, start_time: start, end_time: end }); }); it("handles invalid query time safely", () => { render(<BookingForm locale="en" courtId="9" start={end} end={start} />); fireEvent.click(screen.getByRole("button", { name: "Reserve slot" })); expect(screen.getByRole("alert")).toHaveTextContent("Invalid slot."); }); });
