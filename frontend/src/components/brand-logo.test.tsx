import { render,screen } from "@testing-library/react";import { describe,it,expect } from "vitest";import { BrandLogo } from "./brand-logo";
describe("BrandLogo fallback",()=>{it("has a localized accessible home label",()=>{render(<BrandLogo locale="ar"/>);expect(screen.getByLabelText("ملعبي - الرئيسية")).toHaveAttribute("href","/ar")});it("renders stable text fallback",()=>{render(<BrandLogo locale="en"/>);expect(screen.getByText("Mal3by")).toBeInTheDocument()});it("supports compact fallback",()=>{render(<BrandLogo locale="en" compact/>);expect(screen.getByLabelText("Mal3by home")).toBeInTheDocument()})});
