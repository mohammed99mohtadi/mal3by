import { render,screen } from "@testing-library/react";import { describe,it,expect } from "vitest";import { BrandLogo } from "./brand-logo";
describe("BrandLogo",()=>{it("has a localized accessible home label",()=>{render(<BrandLogo locale="ar"/>);expect(screen.getByLabelText("MAL3ABY - الرئيسية")).toHaveAttribute("href","/ar")});it("renders the official logo asset",()=>{render(<BrandLogo locale="en"/>);expect(screen.getByRole("img",{name:"MAL3ABY"})).toHaveAttribute("src",expect.stringContaining("mal3aby-logo"))});it("supports compact presentation",()=>{render(<BrandLogo locale="en" compact/>);expect(screen.getByLabelText("MAL3ABY home")).toBeInTheDocument()})});
