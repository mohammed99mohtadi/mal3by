"use client";import{useState}from"react";import Link from"next/link";import{Button}from"@/components/ui/button";import{Drawer}from"@/components/ui/drawer";import{Input}from"@/components/ui/input";import{Select}from"@/components/ui/select";import{courtDiscoveryCopy,courtPublicQuery,type CourtFilters}from"@/lib/court-discovery";import type{Locale}from"@/lib/copy";
function Fields({locale,filters,id}:{locale:Locale;filters:CourtFilters;id:string}){const t=courtDiscoveryCopy[locale];return <div className="grid gap-4"><Input id={`${id}-search`} name="search" label={t.search} defaultValue={filters.search} maxLength={100}/><Input id={`${id}-area`} name="area" label={t.area} defaultValue={filters.area} maxLength={100}/><div className="grid grid-cols-2 gap-3"><Input id={`${id}-min`} name="min" type="number" min="0" step="0.001" label={t.min} defaultValue={filters.min}/><Input id={`${id}-max`} name="max" type="number" min="0" step="0.001" label={t.max} defaultValue={filters.max}/></div><label className="grid gap-2 text-sm font-bold" htmlFor={`${id}-active`}>{t.status}<Select id={`${id}-active`} name="active" defaultValue={filters.active} fullWidth><option value="">{t.all}</option><option value="true">{t.active}</option><option value="false">{t.inactive}</option></Select></label><div className="flex gap-2"><Button type="submit" fullWidth>{t.apply}</Button><Link className="focus-ring flex min-h-11 items-center justify-center rounded-[var(--radius-md)] border border-[var(--border-strong)] px-4 font-bold" href={`/${locale}/courts`}>{t.reset}</Link></div></div>}
export function CourtDiscoveryFilters({locale,filters}:{locale:Locale;filters:CourtFilters}){const t=courtDiscoveryCopy[locale],[open,setOpen]=useState(false),active=[filters.search&&["search",filters.search],filters.area&&["area",filters.area],filters.min&&["min",filters.min],filters.max&&["max",filters.max],filters.active&&["active",filters.active==="true"?t.active:t.inactive]].filter(Boolean)as[string,string][];return <><div className="lg:hidden"><Button type="button" variant="outline" fullWidth onClick={()=>setOpen(true)}>{t.openFilters}{active.length?` (${active.length})`:""}</Button></div><aside className="hidden lg:row-span-2 lg:block"><Surface><h2 className="mb-5 text-xl font-black">{t.filters}</h2><form action={`/${locale}/courts`} method="get"><Fields locale={locale} filters={filters} id="desktop"/></form></Surface></aside><Drawer open={open} onClose={()=>setOpen(false)} title={t.filters}><form action={`/${locale}/courts`} method="get"><Fields locale={locale} filters={filters} id="mobile"/></form></Drawer>{active.length>0&&<div className="col-span-full flex flex-wrap gap-2 lg:col-start-2 lg:col-end-3" aria-label={t.activeFilters}>{active.map(([key,label])=>{const next={...filters,[key]:"",page:1}as CourtFilters;return <Link key={key} className="focus-ring rounded-full border border-[var(--brand)]/30 bg-[var(--brand)]/10 px-3 py-2 text-xs font-bold text-[var(--brand)]" href={`/${locale}/courts?${courtPublicQuery(next)}`} aria-label={`${t.reset}: ${label}`}>{label} ×</Link>})}</div>}</>}
import{Surface}from"@/components/ui/surface";
