import Link from "next/link";import { cookies } from "next/headers";import { copy,type Locale } from "@/lib/copy";import { LocaleSwitcher } from "@/components/locale-switcher";
export async function Header({locale}:{locale:string}){const l=locale as Locale;const t=copy[l];const logged=Boolean((await cookies()).get("mal3by_session"));return <header className="sticky top-0 z-20 border-b border-[var(--border)] bg-[#080b0ee8] backdrop-blur"><nav className="mx-auto flex max-w-7xl items-center justify-between gap-3 px-5 py-4"><Link aria-label="Mal3by home" className="flex items-center gap-2 font-black text-white focus-ring" href={`/${l}`}><span className="grid size-8 place-items-center rounded-lg bg-[var(--primary)] text-black">M</span><span className="text-xl">Mal3by</span></Link><div className="hidden items-center gap-5 text-sm font-semibold md:flex"><Link className="text-[var(--muted)] hover:text-white focus-ring" href={`/${l}/courts`}>{t.courts}</Link>{logged&&<Link className="text-[var(--muted)] hover:text-white focus-ring" href={`/${l}/bookings`}>My Bookings</Link>}<LocaleSwitcher locale={l}/>{logged?<><Link className="text-[var(--muted)] hover:text-white focus-ring" href={`/${l}/profile`}>{t.profile}</Link><form action="/api/auth/logout" method="post"><button className="text-[var(--muted)] hover:text-white focus-ring" type="submit">{t.logout}</button></form></>:<Link className="brand-button px-4 py-2 focus-ring" href={`/${l}/login`}>{t.login}</Link>}</div><div className="md:hidden"><LocaleSwitcher locale={l}/></div></nav></header>}
