"use client";import{useRouter}from"next/navigation";import{Button}from"@/components/ui/button";import type{Locale}from"@/lib/copy";export function FeaturedCourtsError({locale}:{locale:Locale}){const router=useRouter(),t=locale==="ar"?{title:"تعذّر تحميل الملاعب المميزة",desc:"باقي الصفحة متاح. حاول تحميل هذا القسم مجددًا.",retry:"إعادة المحاولة"}:{title:"Featured courts could not load",desc:"The rest of the homepage is still available. Retry this section.",retry:"Retry"};return <div className="rounded-[var(--radius-xl)] border border-[var(--danger)]/25 bg-[var(--danger)]/5 p-6 text-center" role="alert"><h3 className="text-lg font-black">{t.title}</h3><p className="mt-2 text-sm text-[var(--text-muted)]">{t.desc}</p><Button className="mt-5" variant="outline" onClick={()=>router.refresh()}>{t.retry}</Button></div>}
