import Link from"next/link";import{StatusBadge}from"@/components/ui/status-badge";import{Surface}from"@/components/ui/surface";import{formatDateTime}from"@/lib/booking-ux";import type{Locale}from"@/lib/copy";import{matchDiscoveryCopy,matchJoinability}from"@/lib/match-discovery";import type{Match}from"@/lib/types";type DiscoveryMatch=Match&{position_requirements?:{position_code:string;required_count:number}[]};const positions={goalkeeper:{en:"Goalkeeper",ar:"حارس مرمى"},defender:{en:"Defender",ar:"مدافع"},midfielder:{en:"Midfielder",ar:"وسط"},forward:{en:"Forward",ar:"مهاجم"}}as const;export function MatchCard({match,locale}:{match:DiscoveryMatch;locale:Locale}){const t=matchDiscoveryCopy[locale],joinability=matchJoinability(match),court=match.court[locale==="ar"?"name_ar":"name_en"],requirements=match.position_requirements??[];return <Link href={`/${locale}/matches/${encodeURIComponent(String(match.id))}`} aria-label={`${t.view}: ${match.title}`} className="focus-ring block min-w-0 rounded-[var(--radius-lg)]"><Surface as="article" variant="interactive" className="grid min-w-0 overflow-hidden p-0 md:grid-cols-[11rem_minmax(0,1fr)]"><div className="flex min-h-36 items-center justify-center bg-[linear-gradient(135deg,var(--surface-2),var(--surface-3))] text-sm font-black text-[var(--text-muted)]" aria-hidden="true">{match.sport_type}</div><div className="min-w-0 p-4 sm:p-5"><div className="flex flex-wrap items-center justify-between gap-2"><div className="flex flex-wrap gap-2"><StatusBadge status={match.status==="open"?"success":match.status==="full"?"warning":"neutral"}>{t.statusLabels[match.status]}</StatusBadge><StatusBadge status="neutral">{t.join[joinability]}</StatusBadge></div><span className="text-xs font-bold text-[var(--brand)]" dir="auto">{match.sport_type}</span></div><h2 className="mt-4 break-words text-xl font-black" dir="auto">{match.title}</h2><p className="mt-2 break-words text-sm text-[var(--text-muted)]"><bdi>{court}</bdi> · <bdi>{match.court.area}</bdi></p><div className="mt-4 grid gap-2 text-sm sm:grid-cols-2"><p><time dateTime={match.start_time}>{formatDateTime(locale,match.start_time)}</time> – <time dateTime={match.end_time}>{formatDateTime(locale,match.end_time,true)}</time></p><p>{t.capacity}: <bdi>{match.approved_participant_count}/{match.max_players}</bdi> · <bdi>{match.available_spots}</bdi> {t.available}</p><p>{t.skill}: {t.skillLabels[match.skill_level]}</p><p>{t.visibility[match.visibility]}</p></div>{requirements.length>0&&<ul className="mt-4 flex flex-wrap gap-2" aria-label={locale==="ar"?"المراكز المطلوبة":"Required positions"}>{requirements.map(item=>{const known=positions[item.position_code as keyof typeof positions];const label=known?known[locale]:item.position_code.replaceAll("_"," ");return <li key={item.position_code} className="rounded-full bg-[var(--surface-3)] px-3 py-1.5 text-xs font-bold"><span dir="auto">{label}</span> ×<bdi>{item.required_count}</bdi></li>})}</ul>}<div className="mt-4 flex flex-wrap items-end justify-between gap-3 border-t border-[var(--border)] pt-4"><p className="text-xs text-[var(--text-muted)]">{t.host}: <bdi>{match.creator.full_name}</bdi></p><span className="font-bold text-[var(--brand)]">{t.view}</span></div></div></Surface></Link>}
