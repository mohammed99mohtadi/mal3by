import{beforeEach,describe,expect,it,vi}from"vitest";import{api}from"./api";describe("C1 courts API",()=>{beforeEach(()=>vi.unstubAllGlobals());it("parses valid real court arrays",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,json:async()=>[{id:1,name_en:"Court",name_ar:"ملعب",area:"Area",price_per_hour:"10",currency:"KWD",is_active:true}]}));await expect(api.courts(new URLSearchParams({limit:"13"}))).resolves.toHaveLength(1);expect(fetch).toHaveBeenCalledWith(expect.stringContaining("/courts?limit=13"),expect.objectContaining({signal:expect.any(AbortSignal)}))});it("rejects malformed successful responses",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,json:async()=>({items:[]})}));await expect(api.courts()).rejects.toMatchObject({status:502})});it("propagates network errors for safe page mapping",async()=>{vi.stubGlobal("fetch",vi.fn().mockRejectedValue(new TypeError("network")));await expect(api.courts()).rejects.toBeInstanceOf(TypeError)})});
