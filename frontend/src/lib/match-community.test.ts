import { describe, expect, it, vi } from "vitest";
import { isUpcoming, lifecycleLabel, matchTone, positionLabel, requestLabel, requestTone } from "./match-community";
import type { Match } from "./types";
describe("CM2 shared presentation",()=>{it("maps every backend lifecycle consistently",()=>{expect(["open","full","cancelled","completed"].map(status=>lifecycleLabel("en",status as Match["status"]))).toEqual(["Open","Match is full","Cancelled","Completed"])});it("maps every request state without raw enums",()=>{expect(["pending","approved","rejected","withdrawn","expired"].map(status=>requestLabel("ar",status as never))).toEqual(["بانتظار الموافقة","مقبول","مرفوض","مسحوب","منتهي الصلاحية"])});it("uses semantic tones",()=>{expect(matchTone("cancelled")).toBe("danger");expect(requestTone("approved")).toBe("success")});it("localizes known positions and safely formats unknown ones",()=>{expect(positionLabel("ar","goalkeeper")).toBe("حارس مرمى");expect(positionLabel("en","wing_back")).toBe("wing back")});it("classifies upcoming from real lifecycle and time",()=>{vi.setSystemTime(new Date("2030-01-01T00:00:00Z"));const match={status:"open",end_time:"2030-01-02T00:00:00Z"} as Match;expect(isUpcoming(match)).toBe(true);expect(isUpcoming({...match,status:"cancelled"})).toBe(false);vi.useRealTimers()})});
