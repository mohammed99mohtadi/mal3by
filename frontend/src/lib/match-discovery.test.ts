import{describe,expect,it}from"vitest";import{backendMatchQuery,filterLoadedMatches,matchJoinability,parseMatchFilters,publicMatchQuery}from"./match-discovery";import type{Match}from"./types";const match={id:1,title:"Friday Football",description:null,sport_type:"football",visibility:"public",join_policy:"approval_required",status:"open",skill_level:"all_levels",min_players:2,max_players:10,start_time:"2030-01-01T10:00:00Z",end_time:"2030-01-01T11:00:00Z",created_at:"2029-01-01T00:00:00Z",creator:{id:2,full_name:"Mona"},court:{id:3,name_en:"Real Court",name_ar:"ملعب حقيقي",area:"Salmiya"},approved_participant_count:4,available_spots:6,has_joined:false,current_user_participant_status:null,can_manage:false,booking_id:4}as Match;describe("match discovery helpers",()=>{it("keeps only supported safe query values",()=>{expect(parseMatchFilters({skill:"expert",status:"deleted",sort:"random",page:"-1",token:"secret"})).toMatchObject({skill:"",status:"",sort:"start_time",page:1});expect(publicMatchQuery(parseMatchFilters({token:"secret"})).has("token")).toBe(false)});it("serializes exact backend filters and pagination",()=>{const query=backendMatchQuery(parseMatchFilters({sport:"football",skill:"advanced",date:"2030-01-02",status:"open",available:"true",page:"2"}));expect(Object.fromEntries(query)).toMatchObject({sport_type:"football",skill_level:"advanced",status:"open",has_available_spots:"true",skip:"12",limit:"13"});expect(query.get("start_date")).toContain("2030-01-02")});it("preserves filters in public pagination URLs",()=>{const filters=parseMatchFilters({sport:"football",page:"2"});expect(publicMatchQuery(filters,{page:3}).toString()).toContain("sport=football");expect(publicMatchQuery(filters,{page:3}).get("page")).toBe("3")});it("searches only supplied loaded results",()=>{expect(filterLoadedMatches([match],"Salmiya")).toHaveLength(1);expect(filterLoadedMatches([match],"Basketball")).toHaveLength(0)});it.each([{change:{can_manage:true},state:"managed"},{change:{current_user_participant_status:"pending"},state:"pending"},{change:{has_joined:true},state:"joined"},{change:{status:"full"},state:"full"},{change:{status:"completed"},state:"closed"},{change:{visibility:"private"},state:"private"},])("maps presentation state $state without authorizing actions",({change,state})=>expect(matchJoinability({...match,...change}as Match)).toBe(state));it("marks only eligible-looking state as open presentation",()=>expect(matchJoinability(match)).toBe("open"))});
