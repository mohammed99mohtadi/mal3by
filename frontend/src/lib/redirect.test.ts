import { describe, expect, it } from "vitest";
import { safeReturnPath } from "./redirect";
describe("safeReturnPath",()=>{it("allows locale-local paths",()=>{expect(safeReturnPath("/ar/courts/123","ar")).toBe("/ar/courts/123")});it("allows an Arabic booking path",()=>expect(safeReturnPath("/ar/bookings/12","ar")).toBe("/ar/bookings/12"));it("preserves valid booking queries",()=>expect(safeReturnPath("/en/bookings/new?courtId=1","en")).toBe("/en/bookings/new?courtId=1"));it("falls back for an empty return path",()=>expect(safeReturnPath(null,"en")).toBe("/en/profile"));it("rejects external and invalid paths",()=>{for(const value of ["https://evil.example","//evil.example","javascript:alert(1)","data:text/html,test","/en/courts"])expect(safeReturnPath(value,"ar")).toBe("/ar/profile")})});
